{
  "name": "oscal-deep-diff",
  "version": "0.0.1",
  "description": "A deep diff tool",
  "main": "./lib/index.js",
  "bin": {
    "oscal-deep-diff": "./lib/index.js"
  },
  "scripts": {
    "start": "nodemon src/index.ts",
    "build": "tsc -p .",
    "test": "mocha -r ts-node/register src/**/*.spec.ts",
    "refresh": "rm -rf ./node_modules ./package-lock.json && npm install"
  },
  "keywords": [
    "oscal"
  ],
  "author": "Nikita Wootten",
  "license": "SEE LICENSE IN LICENSE.md",
  "dependencies": {
    "commander": "^5.1.0"
  },
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.9.1",
    "chai": "^4.2.0",
    "mocha": "^7.1.1",
    "nodemon": "^2.0.2",
    "ts-node": "^8.6.2",
    "typescript": "^3.8.3"
  },
  "nodemonConfig": {
    "watch": "src/**/*.ts",
    "execMap": {
      "ts": "ts-node"
    }
  }
}
